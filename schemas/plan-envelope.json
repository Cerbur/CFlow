{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "cflow://schemas/plan-envelope.json",
  "title": "Plan envelope",
  "description": "YAML front matter contract of a plan Artifact body (PRD: Plan 为不可变、版本化的 Markdown + YAML front matter Artifact). The plan body is a Markdown document that begins with a --- delimited YAML front matter block; the front matter converted to JSON must satisfy this schema. Plan lifecycle status lives only in SQLite; this schema never carries state.",
  "type": "object",
  "additionalProperties": false,
  "required": ["workflow_id", "revision", "title"],
  "properties": {
    "workflow_id": { "type": "string", "minLength": 1 },
    "revision": { "type": "integer", "minimum": 1 },
    "title": { "type": "string", "minLength": 1 },
    "goal": { "type": "string", "minLength": 1 },
    "scope": { "type": "string", "minLength": 1 },
    "constraints": { "type": "string", "minLength": 1 },
    "risk_level": { "type": "string", "enum": ["low", "medium", "high"] },
    "acceptance_strategy": { "type": "string", "minLength": 1 },
    "required_sections": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 1 }
    }
  }
}
